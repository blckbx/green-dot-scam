name: Green Dot Scam
on: [push, pull_request]
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Say hello (and nothing else)
      run: echo "Hello World"  # Boom, green dot earned! 🎉   
